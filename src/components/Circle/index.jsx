import React from 'react';
import './_circle.sass';

const Circle = ({
  style,
}) => {
  return (
    <div
      className="containercircle"
      style={style}
    />
  );
};

export default Circle;
